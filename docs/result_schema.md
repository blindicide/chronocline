# Result schema

`results.csv` is tidy: each row identifies experiment, sweep values, metric name/value, units, replication, estimator, status, and diagnostics. `manifest.json` records software, Git state, seed, configuration hash, timestamps, and generated-file checksums. CSV is mandatory; Parquet is optional.
